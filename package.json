{
  "name": "prototype_select",
  "version": "1.0.0",
  "description": "Accessible enhancements to select HTML elements",
  "main": "rollup.config.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rollup -c && postcss src/css -d dist/css",
    "dev:css": "postcss src/css -d dist/css -w --verbose",
    "dev:js": "rollup -c -w",
    "dev": "concurrently --kill-others \"npm:dev:*\""
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^6.2.0",
    "postcss-cli": "^8.3.1",
    "rollup": "^2.50.2",
    "rollup-plugin-terser": "^7.0.2"
  },
  "dependencies": {
    "postcss": "^8.3.0",
    "postcss-preset-env": "^6.7.0"
  }
}
